[main]
# We want to set up users both from here, and for normal users,
# from the GAE realm.  The only way to set up an admin user is
# from here.

gaeRealm = com.cilogi.shiro.gae.DatastoreRealm
securityManager.realms = $iniRealm, $gaeRealm

[users]
zenith@acme.com = password, user, admin
tim.niblett@cilogi.com = password, user, admin
nadir@acme.com = password, user

[roles]
admin = *
user = browse:*

[urls]
/login.jsp = authc
/settings.html = authc, roles[user]
/listUsers.ftl = authc, roles[admin]
/appstats/** = authc, roles[admin]
/logout = logout
